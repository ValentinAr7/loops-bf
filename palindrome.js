function palindrome(num) {

    let palindrom = true;

        let array = num.split('')
        console.log(array);

        let secondHalf = array.splice(array.length / 2)

        for(let i = 0; i < secondHalf.length; i++){

        }

}

palindrome('123321')